---
bundle:
  name: work-with-images
  display_name: Work · Images
  version: 0.1.0
  description: Work with an optional host-configured image backend.

includes:
  - bundle: work:bundle.md
  - bundle: work:behaviors/work-images.yaml
---

Discover the mounted image tool's capabilities before making an image request.
An installed tool with an unconfigured backend is not an available image service.
