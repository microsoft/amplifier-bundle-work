bundle:
  name: work-images
  display_name: Work · Image tools
  version: 0.1.0
  description: Enable requested paid image generation and editing through a host-selected backend.

# Compatibility entrypoint: the imagegen bundle owns the tool and skill policy.
includes:
  - bundle: git+https://github.com/microsoft/amplifier-bundle-imagegen@main
